# AGENTS.md

## 项目说明

长期迭代的通用前后端分离系统，支持 AI Agent 持续参与开发。

技术栈：React + TypeScript（前端）、Python + FastAPI（后端）、MySQL、Redis、Celery、Docker。

---

## 总体架构原则

- 模块化、高内聚、低耦合、单一职责
- 明确模块边界，最小化模块间直接依赖
- 优先简单、清晰、可维护的设计，避免过度设计
- 禁止循环依赖、跨模块直接访问内部实现

---

## 全局数据原则

业务数据必须可追溯、可恢复、可审计。

**禁止删除数据**：包括硬删除、软删除、`deleted_at`、`is_deleted`、状态模拟删除。涉及删除需求时必须停止并向用户确认。

**数据一致性**：所有变更必须保证事务一致性、数据完整性、并发安全、版本一致性、幂等性。

---

## 测试原则

新增功能必须增加对应测试。完成后运行测试、检查错误、检查 Git Diff、确认未修改无关文件。

---

## Git 规则

- 一个 Commit 对应一个明确的功能或变更
- 不修改历史提交、不强制覆盖远程
- 禁止擅自执行 `git reset --hard`、`git push --force`、`git rebase`

---

## AI Agent 通用规则

必须：先理解再修改、优先复用、保持模块边界、避免重复代码、小步迭代。

禁止：擅自调整架构/删除文件/删除数据、修改 Git 历史、一次实现多个无关功能。

---

## 依赖管理

- 非必要情况下不安装额外的依赖包，优先复用现有依赖与实现。
- 确需安装新依赖时，必须先向用户解释原因，并获得批准后才能执行安装。

---

## 重大变更确认

修改整体架构、核心数据模型、引入新基础设施/大型依赖、修改模块边界/数据保留策略/事务策略/认证体系前，必须先向用户说明方案并等待确认。

---

## Agent 文档读取规则（最小必要上下文）

修改代码时按任务加载所需的最小上下文，禁止无差别读取全部文档。

**修改前端**：`/AGENTS.md` + `/frontend/AGENTS.md`
**修改后端**：`/AGENTS.md` + `/backend/AGENTS.md`

**根据任务按需读取 docs/**：

- 修改某功能 → `/docs/features/<domain>/README.md`
- 涉及数据结构 → `/docs/features/<domain>/data.md`
- 涉及 API → `/docs/features/<domain>/api.md`
- 涉及中间件 → `/docs/middleware/<name>.md`
- 涉及业务规则 → `/docs/business/<name>.md`
- 涉及复杂组件 → `/docs/components/<name>.md`
- 涉及 API 契约 → `/docs/http/api-contract.md`

**禁止**：修改单个功能时读取整个 docs/。

---

## 文档职责边界

- `AGENTS.md`：规则、约束、必须遵守什么
- `docs/`：具体实现、业务知识、技术方案、背景说明

同一条规则只保留在职责更高、范围更准确的文件中，禁止多处重复定义。

---

## 项目优先级

数据正确性 > 安全性 > 事务一致性 > 并发安全 > 可维护性 > 可测试性 > 可扩展性 > 性能 > 开发速度
